const os = require('os')

console.log(os.hostname()) //get the hostname
console.log(os.type()) //get the operating system type
console.log(os.platform()) //get the platform
console.log(os.arch()) //get the architecture
console.log(os.totalmem()) //get the total memory
console.log(os.freemem()) //get the free memory